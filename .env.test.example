# Test Environment Variables
# Copy this file to .env.test and fill in your test values
# IMPORTANT: Never commit actual credentials to git!

# Node Environment  
NODE_ENV=test
TESTING=true

# Test Database Configuration (Required for unit tests)
NEXT_PUBLIC_SUPABASE_TEST_URL=https://your-test-project-ref.supabase.co
SUPABASE_TEST_SERVICE_ROLE_KEY=your-test-service-role-key-here

# Test Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_URL=http://localhost:3001

# Required for some tests - use test/demo keys only
STRIPE_SECRET_KEY=sk_test_your-test-stripe-key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-test-stripe-key

# Test Authentication (use simple test secret)
NEXTAUTH_SECRET=test-secret-key-at-least-32-characters-long
NEXTAUTH_URL=http://localhost:3000

# Optional: Test account credentials (if using hardcoded test accounts)
# Better to use migration test accounts from supabase/migrations/00011_test_accounts.sql
SEED_TEST_PASSWORD=your-test-password-here